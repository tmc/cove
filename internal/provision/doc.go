// Package provision describes staged macOS guest provisioning files.
//
// It records manifests, staging fingerprints, root-owned target checks, username
// validation, and shell escaping shared by the inject and apply paths.
package provision
