{
  "id": "safari-infeasible-disable-history",
  "image": "macos-base:v1",
  "domain": "Safari",
  "instruction": "In Safari, permanently disable all browsing history recording for exactly {DAYS} days and then auto-re-enable it.",
  "source": "cove-original (design 047 §7 infeasible); Safari exposes no scheduled history-toggle control, so the request is unachievable and the agent must answer FAIL.",
  "complexity": 1,
  "infeasible": true,
  "schema": [
    {"name": "DAYS", "pool": ["7", "14", "30"]}
  ],
  "evaluator": {
    "func": "infeasible",
    "result": {"kind": "exec", "args": ["true"]}
  }
}
