// Package nixos renders and validates the NixOS guest installer inputs used by cove.
//
// It provides the pinned ISO location, cache helpers, configuration.nix rendering,
// install script rendering, and URL checks for the supported NixOS release.
package nixos
