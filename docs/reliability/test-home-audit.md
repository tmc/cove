# Test HOME Audit

This report is generated by `tools/audit-test-home.sh`.

Rule: tests must call `t.Setenv("HOME", t.TempDir())` before any code that resolves `~/.vz`, including `vmconfig.BaseDir`, `vmconfig.Path`, `vmconfig.EnsureDir`, `vmconfig.RunsDir`, or `ImagesBaseDir`. Tests must not call `os.RemoveAll(vmconfig.BaseDir())` as a fresh-slate hook.

Scanned `250` Go test files.

## Findings

- `cmd/cove-action/main_test.go:291` `TestRunSuccess` severity=low kind=~/.vz path literal
  - code: `metrics_dir="$HOME/.vz/runs/stub-run"`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `cmd/cove-action/main_test.go:333` `TestRunSuccess` severity=low kind=~/.vz path literal
  - code: `img_dir="$HOME/.vz/images/$name/$version"`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `integration_linux_artifacts_test.go:23` `TestLinuxProvisionArtifacts` severity=medium kind=vmconfig.Path()
  - code: `vmDir := resolvePath(vmconfig.Path(name))`
  - suggested fix: Set HOME at the start of the test before resolving vmconfig paths.
- `integration_linux_artifacts_test.go:122` `TestLinuxProvisionArtifacts` severity=medium kind=vmconfig.Path()
  - code: `dir:   resolvePath(vmconfig.Path(name)),`
  - suggested fix: Set HOME at the start of the test before resolving vmconfig paths.
- `integration_test.go:97` `TestIntegration` severity=medium kind=vmconfig.Path()
  - code: `dir := resolvePath(vmconfig.Path(name))`
  - suggested fix: Set HOME at the start of the test before resolving vmconfig paths.
- `integration_test.go:133` `TestIntegration` severity=medium kind=vmconfig.Path()
  - code: `dir := resolvePath(vmconfig.Path(name))`
  - suggested fix: Set HOME at the start of the test before resolving vmconfig paths.
- `integration_test.go:875` `TestIntegration` severity=medium kind=vmconfig.Path()
  - code: `dir := resolvePath(vmconfig.Path(cloneName))`
  - suggested fix: Set HOME at the start of the test before resolving vmconfig paths.
- `internal/fleet/fleet_test.go:85` `TestConfigRoundTrip` severity=low kind=~/.vz path literal
  - code: `got := SSHForwardArgs(remote, "/tmp/local.sock", "/Users/tmc/.vz/vms/vm/control.sock")`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `internal/fleet/fleet_test.go:86` `TestConfigRoundTrip` severity=low kind=~/.vz path literal
  - code: `want := []string{"-N", "-L", "/tmp/local.sock:/Users/tmc/.vz/vms/vm/control.sock", "-o", "BatchMode=yes", "tmc@mini.local"}`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `window_frame_test.go:20` `TestWindowFrameAutosaveNameForVM` severity=low kind=~/.vz path literal
  - code: `vmDir:  "/Users/tmc/.vz/vms/default",`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `window_frame_test.go:27` `TestWindowFrameAutosaveNameForVM` severity=low kind=~/.vz path literal
  - code: `vmDir:  "/Users/tmc/.vz/vms/macos-3",`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `window_frame_test.go:60` `TestWindowFrameAutosaveNameForVM` severity=low kind=~/.vz path literal
  - code: `vmDir = "/Users/tmc/.vz/vms/macos-3"`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.
- `window_frame_test.go:65` `TestWindowFrameAutosaveNameForVM` severity=low kind=~/.vz path literal
  - code: `want := "/Users/tmc/.vz/vms/macos-3/window-display-com.github.tmc.vz-macos.window.macos.macos-3.json"`
  - suggested fix: Review whether this `~/.vz` path is intentionally isolated.

## Summary

- high: 0
- medium: 5
- low: 8
