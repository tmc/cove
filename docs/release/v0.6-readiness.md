---
title: cove v0.6.0 readiness audit
status: Ready pending user decision
date: 2026-05-09
---

# cove v0.6.0 readiness audit

This audit records the release-prep gates for the v0.6.0 tag. It is a
pre-tag readiness result, not approval to cut. The tag is **user-gated**
following the same convention used for v0.5 (`#224`, `#225`, `#294`).

## Headline

v0.6 raises the macOS host floor from implicit 12.0 to **14.0 (Sonoma)**
and lands design 041 ScreenCaptureKit Slices 1-3 (probe, spike, dual-path
opt-in). Slice 4 (default flip + `CGWindowListCreateImage` retire) is
deferred to v0.7 pending Slice 2 perf data. v0.6 also closes the last
purely-engineering Cirrus migration blocker by routing
`ENCRYPTED[…]` secrets through `cove shell --secret-env` and
`cove-action`'s `secrets:` input. Polish landmarks: design 040 storage
budget Phases 0-5, 53 `-h` CLI bugs squashed (R81-R84), `cove doctor`
E2E coverage (R76-R80), `cove-action` cache-mode + cache-scope, storage
census PiB int64 overflow fix.

## Anchor

- **Recommended cut SHA**: `6a67714` (`cove-action: test cache-mode and
  cache-scope flag handling`) — current `origin/main` HEAD.
- **v0.5 anchor for cumulative diff**: `463c5ce` (tag object `8e4c767`,
  see `docs/release/v0.5-readiness.md`).
- **Cumulative diff**: 100 commits `v0.5.0..origin/main` (`6a67714`).

## Required checks

- [x] `RELEASE-NOTES-v0.6.0.md` exists at repo root with shipped scope
      and SHA-anchored slice rows. R85 addendum already landed.
- [x] `go build ./...` passes at `6a67714`.
- [ ] `go test ./...` — not run by this audit (doc-only worktree); cutter
      must re-run before tag per release-cut sequence below.
- [x] Design 041 Slices 1-3 landed: `8d55d7a` (probe), `d0877b8` (spike),
      `55257f2` (dual-path). Slice 4 spec at `318d801` deferred to v0.7.
- [x] Cirrus secrets Slices 1-2 landed: `fe99629` (`cove shell
      --secret-env`), `c9df361` (`cove-action` `secrets:` input).
- [x] Design 040 storage budget Phases 0-5 shipped on main (memory
      reference: `project_design_040_shipped_r57.md`).
- [x] CLI `-h` regression net: 53 bugs across R81/R83-R84 with regression
      tests guarding each path.
- [x] `cove doctor` E2E expanded to version/image/vm/pin/runs
      (`327e4ba`, `caada88`).
- [x] Image gc race audit closed R1-R5/R7 at `a1ccf54`; R6 cache TTL
      deferred (not a tag blocker).
- [x] Storage census PiB int64 overflow fixed at `c7865e1`.
- [x] No P0 blocker identified for v0.6 by this audit.

## User-gated items

- [ ] Tag not yet cut. Cut sequence below requires explicit user
      approval.
- [ ] Privacy gate stands. cove repo is **private**. No public push to
      `tmc/homebrew-tap`, nixpkgs, public OCI registries, or any surface
      that would 404 for non-collaborators.
- [ ] Slice 2 SCKit perf numbers (p50/p95) blocked on Screen Recording
      TCC grant on the bench host. Numbers are needed before Slice 4
      can ship in v0.7; not a v0.6 tag blocker.
- [ ] Quiet-host SCKit dual-path A/B re-run desirable post-tag for the
      v0.7 Slice 4 decision. Not a tag blocker.

## Release-cut command sequence

Run from a release-prep worktree on the merge commit you intend to tag.

```bash
cd /Volumes/tmc/go/src/github.com/tmc/vz-macos
git checkout main
git pull --ff-only origin main

# Verify cut anchor.
git rev-parse HEAD   # must equal 6a67714 or descend from it on main

# Pre-flight: build, sign, test.
go build -o cove .
codesign -s - -f --entitlements internal/autosign/vz.entitlements ./cove
go test ./...

# Smoke: ./cove --version, ./cove doctor, ./cove image list,
#        ./cove doctor sckit-preauth (informational with default cgwindow).

# Tag (user-gated).
git tag -a v0.6.0 -m "cove v0.6.0"
# DO NOT push to origin until user approves the tag cut.
git push origin v0.6.0
```

## Pre-flight verifications

Before pushing the tag:

1. Confirm `main` HEAD matches `6a67714` or any descendant containing
   only doc/release-prep commits.
2. Re-run `go test ./...` including the new `internal/sckit` package
   tests. Any failure halts the cut.
3. Re-sign `./cove` with `internal/autosign/vz.entitlements` after
   build. The Virtualization framework refuses to load on first run if
   the binary is not properly signed.
4. Smoke `./cove --version`, `./cove doctor`, `./cove image list`,
   `./cove doctor sckit-preauth`. None should panic; doctor exit code
   may be nonzero on hosts missing optional state (TCC, Apple Events,
   sandbox) — acceptable for the cut.
5. Confirm `RELEASE-NOTES-v0.6.0.md` and this readiness doc are
   committed on the tag commit.
6. Verify host floor messaging: a 13.x host should see the 14.0+
   advisory on first SCKit capture (paths gated; default `cgwindow`
   leaves 13.x untouched).

## Privacy gate (load-bearing)

cove repo is **private**. Until the user flips it public, do **not**:

- Push to `tmc/homebrew-tap`.
- Push to `nixpkgs`.
- Push images to public OCI registries.
- Reference public cove URLs in any Cask / formula / package metadata.

Internal feature work and private tag pushes are fine. The `git push
origin v0.6.0` step above pushes to the private cove origin only.

## Risk callouts

| Risk | Severity | Mitigation |
|---|---|---|
| 13.x host hits 14.0 floor unexpectedly | Medium | Default capture stays `cgwindow`; only `COVE_CAPTURE_BACKEND=sckit` opt-in surfaces the 14.0 advisory. Release notes call this out. |
| Slice 2 perf data still TBD | Low | Slice 4 explicitly deferred to v0.7; v0.6 ships dual-path opt-in only. |
| Secret leakage via `--secret-env` | Medium | Metrics redactor scrubs resolved values from `cove runs` logs. No in-band stdout/stderr masking; documented in RELEASE-NOTES. |
| Image gc R6 cache TTL still open | Low | Deferred; not on the gc-race hot path. |
| `go test ./...` not run in this audit | Medium | Cutter must re-run pre-tag; release-cut sequence above mandates it. |

## Rollback posture

A v0.6.0 tag annotated against `6a67714` (or descendant). If a post-tag
smoke uncovers a P0:

1. **Tag deletion** (cheap, no public consumers because privacy gate
   stands):
   ```bash
   git tag -d v0.6.0
   git push --delete origin v0.6.0
   ```
2. Land the fix on `main`, retag at the new HEAD.

If the privacy gate has been flipped before discovery:

1. Cut `v0.6.1` on `main` with the fix.
2. Update `RELEASE-NOTES-v0.6.0.md` to record the issue and link to
   v0.6.1.
3. Do not delete the public tag.

## What this audit does NOT cover

- Tag-cut decisions for v0.2.1, v0.3.0, v0.4.0 (independent audits).
- Public infrastructure pushes (privacy gate forbids).
- v0.7 scope, including design 041 Slice 4 default flip and
  `CGWindowListCreateImage` retire.
- SCKit Slice 2 quiet-host perf numbers — blocked on TCC grant.
