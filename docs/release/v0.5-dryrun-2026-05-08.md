# v0.5 pre-tag dry run (2026-05-08)

What would happen if we cut `v0.5.0` right now. Read in 90 seconds, decide
go/no-go.

## 1. Tag target

- HEAD: `0d663d9` on `origin/main` (`docs: refresh design status lines`).
- Previous tag: `v0.1.3` (the last-cut tag on this repo; v0.2-v0.4 shipped
  without git tags per the coordinated-loop convention).
- Range: `v0.1.3..0d663d9` = **572 commits**.

## 2. Diff stats

```
git diff v0.1.3..HEAD --shortstat
755 files changed, 73554 insertions(+), 5309 deletions(-)
```

Tail of `--stat`:

```
 vzscripts/xcode-mas.vzscript                       |    1 +
 vzscripts/xcode.vzscript                           |   16 +-
 windows.go                                         |  795 +++++++++++
 windows_test.go                                    |  174 +++
```

## 3. Test gate

Captured fresh in the dry-run worktree at HEAD `0d663d9`.

| Command | Result | Wall |
|---|---|---|
| `go build ./...` | passed | 3.9s |
| `go vet ./...` | passed | 1.0s |
| `go test ./...` | passed (42/42 packages, 0 failures) | 50.0s |

No `FAIL` or `--- FAIL` lines in the test log. The two long-carried
provisioning tests cleared at `1f8435c` are still green.

## 4. CI status

`gh run list --workflow=pr --limit 5` (as observed):

```
completed  failure  release-notes: v0.6 fill SCKit Slice 1 row     5s  2026-05-08
completed  failure  docs: refresh design status lines              5s  2026-05-08
completed  failure  storagecensus: add table-driven test for...    5s  2026-05-08
completed  failure  docs: cirrus migration readiness audit         5s  2026-05-08
completed  failure  sckit: add SCScreenshotManager spike           5s  2026-05-08
```

All five runs aborted in 5–6s. Per memory
`feedback_verify_ci_actually_running.md`, this signature (very short
"failure" with no test phase) is **billing-blocked**, not real test
failure. CI has not run a real job in this window. Local
`go build` + `go vet` + `go test ./...` are the only ground-truth
signals.

## 5. Designs shipped between v0.1.3 and HEAD

Cross-checked against `docs/designs/` status lines.

| Design | Status | Anchor |
|---|---|---|
| 013 fork (Phases 3-5) | Shipped | `99b3732`, `eacbf5e`, `4f8035c` |
| 022 anthropic adapter | Shipped | `5efff59` |
| 023 cove shell | Shipped | `17211bd` |
| 024 runner images | Shipped (Slices 1-2) | `8a106dc` |
| 027 disk-io tuning Slice 4 | Shipped | `fc7ff1e`, `a459076` |
| 028 block-device passthrough | Shipped | `b522ab3`, `a78e891`, `74d9527` |
| 030 GHA executor Slice 2 | Shipped | `9e6253a`, `f06d554`, `c0a1433` |
| 034 agent sandbox v2 | Shipped | `3c4a082`, `b3d71ab`, `14ab331` |
| 039 package boundary | Shipped | `8bd7a65` (R49 arc) |
| 040 storage budget | Shipped (all phases) | `6cf3a93` |
| 041 SCKit migration | Accepted; horizon v0.6 | `50bf8ca` (design only) |

041 is **not** a v0.5 deliverable; it is the headline of v0.6 and is
captured in `RELEASE-NOTES-v0.6.0.md` (scaffold-only).

## 6. Open issues

```
gh issue list --limit 30 --state open --json number,title,labels
[]
```

No open GitHub issues. Tag-cut gate issues `#224`, `#225`, `#294`
referenced in `docs/release/v0.5-readiness.md` are user-owned approvals,
not GitHub-issue blockers.

## 7. Privacy gate

cove repo is private. The v0.1.3..HEAD diff contains:

- `dist/Casks/cove-v0.2.1.rb` and `dist/Casks/cove-v0.3.0.rb` — local
  cask files only, not pushed to `tmc/homebrew-tap`. Verified clean per
  memory `feedback_no_public_pushes_while_private.md`.
- No nixpkgs PRs.
- No public OCI registry pushes (design 024 Slice 3 deferred).

Privacy gate intact.

## 8. Recommendation

**GO** — local `go test ./...` is 42/42 green at `0d663d9`, designs 039
+ 040 ship cleanly, privacy gate intact, no open issues. CI is
billing-blocked but that is a sustained pre-existing condition, not a
v0.5 regression. The single tip is: if the user is comfortable cutting
on local-test-only signal (memory says they have been throughout the
v0.4 arc), the cut is ready.

Next concrete action if NO-GO: re-enable GHA billing so a real CI run
can attest the same green that local already shows; nothing else
blocks.
