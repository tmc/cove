# macOS Tahoe (26.x) Setup Assistant boot commands
# These navigate Setup Assistant when disk injection is not available.
# Uses OCR click commands for reliable navigation across language variants.

# Wait for initial boot
<wait 30s>

# Hello screen — click Continue (visible in any language)
<waitForText "Continue">
<click "Continue">
<wait 3s>

# Language selection — English should be highlighted by default
# Click the arrow button or press Return to accept English
<waitForText "Language">
<wait 1s>
<key return>
<wait 3s>

# Country/Region — select and continue
<waitForText "Country">
<wait 1s>
<key return>
<wait 3s>

# Accessibility — skip
<waitForText "Accessibility">
<wait 1s>
<click "Not Now">
<wait 3s>

# Wi-Fi — skip (VM uses NAT networking)
<waitForText "Wi-Fi">
<wait 1s>
<click "Other Network Options">
<wait 2s>
<click "without">
<wait 2s>
<click "Continue">
<wait 3s>

# Migration Assistant — Don't transfer
<waitForText "Migration">
<wait 1s>
<click "Not Now">
<wait 3s>

# Apple ID — Skip / Set Up Later
<waitForText "Apple">
<wait 1s>
<click "Set Up Later">
<wait 2s>
<click "Skip">
<wait 3s>

# Terms and Conditions — Agree
<waitForText "Terms">
<wait 1s>
<click "Agree">
<wait 2s>
# Confirmation dialog
<click "Agree">
<wait 3s>

# Create Computer Account — fill in user details
# The orchestrator will type username/password after this
<waitForText "Account">
<screenshot>
