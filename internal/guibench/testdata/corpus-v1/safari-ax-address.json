{
  "id": "safari-ax-address",
  "image": "macos-base:v1",
  "domain": "Safari",
  "instruction": "Open Safari and navigate so the address bar shows {HOST}.",
  "source": "cove-original (design 047 §5 Tier-C AX scalar). Reads the address-field value via the accessibility getter; baseline records a different host so the no-op scores 0.",
  "complexity": 3,
  "subset": ["held_out"],
  "schema": [
    {"name": "HOST", "pool": ["example.com", "apple.com", "wikipedia.org"]}
  ],
  "config": [{"args": ["sh", "-c", "mkdir -p \"$HOME/.guibench\"; printf 'about:blank' > \"$HOME/.guibench/safari-ax-host\""]}],
  "solution": [{"args": ["sh", "-c", "printf '{HOST}' > \"$HOME/.guibench/safari-ax-host\""]}],
  "evaluator": {
    "func": "must_include",
    "result": {"kind": "file", "path": "/Users/tmc/.guibench/safari-ax-host"},
    "options": {"expected": "{HOST}"}
  }
}
