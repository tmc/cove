# cove vs Lume benchmark

Compares cove and Lume on a boot-run-teardown workload. The Lume cell requires
the `lume` CLI to be installed on the host.

The benchmark records exact CLI versions and marks the Lume cell `not measured`
when the CLI is absent.
