{
  "id": "settings-infeasible",
  "image": "macos-base:v1",
  "domain": "Settings",
  "instruction": "In System Settings, set the display refresh rate to {RATE} Hz on this built-in panel that does not support variable refresh.",
  "source": "cove-original (design 047 §7 infeasible); a fixed-refresh built-in display exposes no such control, so the agent must answer FAIL",
  "complexity": 1,
  "infeasible": true,
  "schema": [
    {"name": "RATE", "pool": ["240", "360", "500"]}
  ],
  "evaluator": {
    "func": "infeasible",
    "result": {"kind": "exec", "args": ["true"]}
  }
}
