---
title: cove v0.4.0 readiness audit
status: Ready for user-gated tag approval
date: 2026-05-05
---

# cove v0.4.0 readiness audit

This audit records the release-prep gates for the v0.4.0 tag. It is a
pre-tag readiness result, not approval to cut the tag.

## Required checks

- [x] `git status --short --branch` showed only the expected release-prep
      branch state before this file was edited:
      `## conductor/r41-ee85-v04-closeout...origin/main [ahead 3]`.
- [x] `git log --oneline origin/main..HEAD` showed only release-prep commits:
      `109d58d docs: update v0.4 cli reference`,
      `3ef3c42 docs: finalize v0.4 release notes`, and
      `cc7fcbf docs: add v0.4 roadmap closeout`.
- [x] `RELEASE-NOTES-v0.4.0.md`, `docs/integrations/matrix.md`, and
      `docs/reference/release-checklist.md` are readable.
- [x] `RELEASE-NOTES-v0.4.0.md` and `docs/designs/ROADMAP.md` cite only commit
      hashes that resolve in the local repository.
- [x] The release notes, roadmap, and CLI reference cover the shipped v0.4
      surface. The design indexes are tracked in the next release-prep commit.
- [x] `go build -o cove .` passes.
- [x] `codesign -s - -f --entitlements internal/autosign/vz.entitlements ./cove`
      passes.
- [x] `go test ./...` passes.
- [x] The release checklist in `docs/reference/release-checklist.md` still
      describes a user-approved tag flow.
- [x] No P0 blocker is called out by this closeout pass. Remaining release notes
      items are tracked as known issues or deferred follow-ups below.

## User-gated items

- [ ] Do not cut the tag until issue `#224` is approved.
- [ ] Do not cut the tag until issue `#225` is approved.

## Release notes to keep visible

- `#237` still tracks Ubuntu Desktop first-boot reliability.
- `#232` still tracks vmstate Phase 5 follow-on status.
- Public OCI distribution remains private.
- Cirrus migration remains a doc draft, not an automatic conversion path.

## Gate output

```text
$ go build -o cove .
$ codesign -s - -f --entitlements internal/autosign/vz.entitlements ./cove
./cove: replacing existing signature
$ go test ./...
ok  	github.com/tmc/vz-macos	28.349s
ok  	github.com/tmc/vz-macos/cmd/cove-action	14.553s
ok  	github.com/tmc/vz-macos/cmd/coved	4.062s
ok  	github.com/tmc/vz-macos/cmd/fork-bench	0.861s
ok  	github.com/tmc/vz-macos/cmd/soft-reset-matrix	1.390s
ok  	github.com/tmc/vz-macos/cmd/vz-agent	4.495s
ok  	github.com/tmc/vz-macos/internal/action	3.018s
ok  	github.com/tmc/vz-macos/internal/agent	2.813s
ok  	github.com/tmc/vz-macos/internal/agentsandbox	1.155s
ok  	github.com/tmc/vz-macos/internal/anthropicadapter	3.406s
...
ok  	github.com/tmc/vz-macos/internal/vmquota	4.186s
ok  	github.com/tmc/vz-macos/internal/windows	4.909s
ok  	github.com/tmc/vz-macos/internal/windows/esd	4.730s
```

## Final sign-off shape

Before tagging:

1. verify the repo state again;
2. verify the release notes and matrix against the current branch tip;
3. verify the build and test gates;
4. confirm the user-gated items;
5. tag only after the operator says to proceed.
