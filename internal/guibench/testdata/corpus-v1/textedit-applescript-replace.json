{
  "id": "textedit-applescript-replace",
  "image": "macos-base:v1",
  "domain": "TextEdit",
  "instruction": "In TextEdit, replace the document body so it reads exactly {PHRASE}.",
  "source": "cove-original (design 047 §5 Tier-C Apple Events). Reads the document text via AppleScript; the baseline sets a different body so the no-op scores 0.",
  "complexity": 3,
  "subset": ["held_out"],
  "schema": [
    {"name": "PHRASE", "pool": ["Final draft ready", "Approved for release", "Pending review", "Archived copy"]},
    {"name": "BASELINE", "expected_from": "PHRASE", "derive": {"Final draft ready": "placeholder text", "Approved for release": "placeholder text", "Pending review": "placeholder text", "Archived copy": "placeholder text"}}
  ],
  "config": [
    {"args": ["sh", "-c", "osascript -e 'tell application \"TextEdit\" to activate' -e 'tell application \"TextEdit\" to if (count of documents) = 0 then make new document' -e 'tell application \"TextEdit\" to set text of front document to \"{BASELINE}\"' 2>/dev/null || true"]}
  ],
  "solution": [
    {"args": ["sh", "-c", "osascript -e 'tell application \"TextEdit\" to set text of front document to \"{PHRASE}\"' 2>/dev/null || true"]}
  ],
  "evaluator": {
    "func": "exact_match",
    "result": {"kind": "applescript", "script": "tell application \"TextEdit\" to get text of front document"},
    "options": {"expected": "{PHRASE}"}
  }
}
